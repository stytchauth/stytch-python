# !!!
# WARNING: This file is autogenerated
# Only modify code within MANUAL() sections
# or your changes may be overwritten later!
# !!!

from typing import Any, Dict, Optional

from stytch.core.api_base import ApiBase
from stytch.core.http.client import AsyncClient, SyncClient
from stytch.models.totp_service import (
    TotpsauthenticateResponse,
    TotpscreateResponse,
    TotpsgetrecoverycodesResponse,
    TotpsrecoverResponse,
)


class TOTPService:
    def __init__(
        self,
        api_base: ApiBase,
        sync_client: SyncClient,
        async_client: AsyncClient,
    ) -> None:
        self.api_base = api_base
        self.sync_client = sync_client
        self.async_client = async_client

    @property
    def sub_url(self) -> str:
        return "totp_service"

    def TOTPsCreate(
        self,
        user_id: str,
        expiration_minutes: Optional[int] = None,
    ) -> TotpscreateResponse:
        payload: Dict[str, Any] = {
            "user_id": user_id,
        }

        if expiration_minutes is not None:
            payload["expiration_minutes"] = expiration_minutes

        url = self.api_base.route_with_sub_url(self.sub_url, "/v1/totps")

        res = self.sync_client.post(url, json=payload)
        return TotpscreateResponse.from_json(res.response.status_code, res.json)

    async def TOTPsCreate_async(
        self,
        user_id: str,
        expiration_minutes: Optional[int] = None,
    ) -> TotpscreateResponse:
        payload: Dict[str, Any] = {
            "user_id": user_id,
        }

        if expiration_minutes is not None:
            payload["expiration_minutes"] = expiration_minutes

        url = self.api_base.route_with_sub_url(self.sub_url, "/v1/totps")

        res = await self.async_client.post(url, json=payload)
        return TotpscreateResponse.from_json(res.response.status, res.json)

    def TOTPsAuthenticate(
        self,
        user_id: str,
        totp_code: str,
        session_token: Optional[str] = None,
        session_duration_minutes: Optional[int] = None,
        session_jwt: Optional[str] = None,
        session_custom_claims: Optional[Dict[str, str]] = None,
    ) -> TotpsauthenticateResponse:
        payload: Dict[str, Any] = {
            "user_id": user_id,
            "totp_code": totp_code,
        }

        if session_token is not None:
            payload["session_token"] = session_token
        if session_duration_minutes is not None:
            payload["session_duration_minutes"] = session_duration_minutes
        if session_jwt is not None:
            payload["session_jwt"] = session_jwt
        if session_custom_claims is not None:
            payload["session_custom_claims"] = session_custom_claims

        url = self.api_base.route_with_sub_url(self.sub_url, "/v1/totps/authenticate")

        res = self.sync_client.post(url, json=payload)
        return TotpsauthenticateResponse.from_json(res.response.status_code, res.json)

    async def TOTPsAuthenticate_async(
        self,
        user_id: str,
        totp_code: str,
        session_token: Optional[str] = None,
        session_duration_minutes: Optional[int] = None,
        session_jwt: Optional[str] = None,
        session_custom_claims: Optional[Dict[str, str]] = None,
    ) -> TotpsauthenticateResponse:
        payload: Dict[str, Any] = {
            "user_id": user_id,
            "totp_code": totp_code,
        }

        if session_token is not None:
            payload["session_token"] = session_token
        if session_duration_minutes is not None:
            payload["session_duration_minutes"] = session_duration_minutes
        if session_jwt is not None:
            payload["session_jwt"] = session_jwt
        if session_custom_claims is not None:
            payload["session_custom_claims"] = session_custom_claims

        url = self.api_base.route_with_sub_url(self.sub_url, "/v1/totps/authenticate")

        res = await self.async_client.post(url, json=payload)
        return TotpsauthenticateResponse.from_json(res.response.status, res.json)

    def TOTPsGetRecoveryCodes(
        self,
        user_id: str,
    ) -> TotpsgetrecoverycodesResponse:
        payload: Dict[str, Any] = {
            "user_id": user_id,
        }

        url = self.api_base.route_with_sub_url(self.sub_url, "/v1/totps/recovery_codes")

        res = self.sync_client.post(url, json=payload)
        return TotpsgetrecoverycodesResponse.from_json(
            res.response.status_code, res.json
        )

    async def TOTPsGetRecoveryCodes_async(
        self,
        user_id: str,
    ) -> TotpsgetrecoverycodesResponse:
        payload: Dict[str, Any] = {
            "user_id": user_id,
        }

        url = self.api_base.route_with_sub_url(self.sub_url, "/v1/totps/recovery_codes")

        res = await self.async_client.post(url, json=payload)
        return TotpsgetrecoverycodesResponse.from_json(res.response.status, res.json)

    def TOTPsRecover(
        self,
        user_id: str,
        recovery_code: str,
        session_token: Optional[str] = None,
        session_duration_minutes: Optional[int] = None,
        session_jwt: Optional[str] = None,
        session_custom_claims: Optional[Dict[str, str]] = None,
    ) -> TotpsrecoverResponse:
        payload: Dict[str, Any] = {
            "user_id": user_id,
            "recovery_code": recovery_code,
        }

        if session_token is not None:
            payload["session_token"] = session_token
        if session_duration_minutes is not None:
            payload["session_duration_minutes"] = session_duration_minutes
        if session_jwt is not None:
            payload["session_jwt"] = session_jwt
        if session_custom_claims is not None:
            payload["session_custom_claims"] = session_custom_claims

        url = self.api_base.route_with_sub_url(self.sub_url, "/v1/totps/recover")

        res = self.sync_client.post(url, json=payload)
        return TotpsrecoverResponse.from_json(res.response.status_code, res.json)

    async def TOTPsRecover_async(
        self,
        user_id: str,
        recovery_code: str,
        session_token: Optional[str] = None,
        session_duration_minutes: Optional[int] = None,
        session_jwt: Optional[str] = None,
        session_custom_claims: Optional[Dict[str, str]] = None,
    ) -> TotpsrecoverResponse:
        payload: Dict[str, Any] = {
            "user_id": user_id,
            "recovery_code": recovery_code,
        }

        if session_token is not None:
            payload["session_token"] = session_token
        if session_duration_minutes is not None:
            payload["session_duration_minutes"] = session_duration_minutes
        if session_jwt is not None:
            payload["session_jwt"] = session_jwt
        if session_custom_claims is not None:
            payload["session_custom_claims"] = session_custom_claims

        url = self.api_base.route_with_sub_url(self.sub_url, "/v1/totps/recover")

        res = await self.async_client.post(url, json=payload)
        return TotpsrecoverResponse.from_json(res.response.status, res.json)

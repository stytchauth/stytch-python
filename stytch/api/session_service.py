# !!!
# WARNING: This file is autogenerated
# Only modify code within MANUAL() sections
# or your changes may be overwritten later!
# !!!

from typing import Any, Dict, Optional

from stytch.core.api_base import ApiBase
from stytch.core.http.client import AsyncClient, SyncClient
from stytch.models.session_service import (
    MultitenantsessionsjwksResponse,
    SessionsauthenticateResponse,
    SessionsgetResponse,
    SessionsjwksResponse,
    SessionsrevokeResponse,
)


class SessionService:
    def __init__(
        self,
        api_base: ApiBase,
        sync_client: SyncClient,
        async_client: AsyncClient,
    ) -> None:
        self.api_base = api_base
        self.sync_client = sync_client
        self.async_client = async_client

    @property
    def sub_url(self) -> str:
        return "session_service"

    def SessionsGet(
        self,
        user_id: str,
    ) -> SessionsgetResponse:
        payload: Dict[str, Any] = {
            "user_id": user_id,
        }

        url = self.api_base.route_with_sub_url(self.sub_url, "/v1/sessions")

        res = self.sync_client.get(url, params=payload)
        return SessionsgetResponse.from_json(res.response.status_code, res.json)

    async def SessionsGet_async(
        self,
        user_id: str,
    ) -> SessionsgetResponse:
        payload: Dict[str, Any] = {
            "user_id": user_id,
        }

        url = self.api_base.route_with_sub_url(self.sub_url, "/v1/sessions")

        res = await self.async_client.get(url, params=payload)
        return SessionsgetResponse.from_json(res.response.status, res.json)

    def SessionsAuthenticate(
        self,
        session_token: Optional[str] = None,
        session_duration_minutes: Optional[int] = None,
        session_jwt: Optional[str] = None,
        session_custom_claims: Optional[Dict[str, Any]] = None,
    ) -> SessionsauthenticateResponse:
        payload: Dict[str, Any] = {}

        if session_token is not None:
            payload["session_token"] = session_token
        if session_duration_minutes is not None:
            payload["session_duration_minutes"] = session_duration_minutes
        if session_jwt is not None:
            payload["session_jwt"] = session_jwt
        if session_custom_claims is not None:
            payload["session_custom_claims"] = session_custom_claims

        url = self.api_base.route_with_sub_url(
            self.sub_url, "/v1/sessions/authenticate"
        )

        res = self.sync_client.post(url, json=payload)
        return SessionsauthenticateResponse.from_json(
            res.response.status_code, res.json
        )

    async def SessionsAuthenticate_async(
        self,
        session_token: Optional[str] = None,
        session_duration_minutes: Optional[int] = None,
        session_jwt: Optional[str] = None,
        session_custom_claims: Optional[Dict[str, Any]] = None,
    ) -> SessionsauthenticateResponse:
        payload: Dict[str, Any] = {}

        if session_token is not None:
            payload["session_token"] = session_token
        if session_duration_minutes is not None:
            payload["session_duration_minutes"] = session_duration_minutes
        if session_jwt is not None:
            payload["session_jwt"] = session_jwt
        if session_custom_claims is not None:
            payload["session_custom_claims"] = session_custom_claims

        url = self.api_base.route_with_sub_url(
            self.sub_url, "/v1/sessions/authenticate"
        )

        res = await self.async_client.post(url, json=payload)
        return SessionsauthenticateResponse.from_json(res.response.status, res.json)

    def SessionsRevoke(
        self,
        session_id: Optional[str] = None,
        session_token: Optional[str] = None,
        session_jwt: Optional[str] = None,
    ) -> SessionsrevokeResponse:
        payload: Dict[str, Any] = {}

        if session_id is not None:
            payload["session_id"] = session_id
        if session_token is not None:
            payload["session_token"] = session_token
        if session_jwt is not None:
            payload["session_jwt"] = session_jwt

        url = self.api_base.route_with_sub_url(self.sub_url, "/v1/sessions/revoke")

        res = self.sync_client.post(url, json=payload)
        return SessionsrevokeResponse.from_json(res.response.status_code, res.json)

    async def SessionsRevoke_async(
        self,
        session_id: Optional[str] = None,
        session_token: Optional[str] = None,
        session_jwt: Optional[str] = None,
    ) -> SessionsrevokeResponse:
        payload: Dict[str, Any] = {}

        if session_id is not None:
            payload["session_id"] = session_id
        if session_token is not None:
            payload["session_token"] = session_token
        if session_jwt is not None:
            payload["session_jwt"] = session_jwt

        url = self.api_base.route_with_sub_url(self.sub_url, "/v1/sessions/revoke")

        res = await self.async_client.post(url, json=payload)
        return SessionsrevokeResponse.from_json(res.response.status, res.json)

    def SessionsJwks(
        self,
        project_id: str,
    ) -> SessionsjwksResponse:
        payload: Dict[str, Any] = {
            "project_id": project_id,
        }

        url = self.api_base.route_with_sub_url(
            self.sub_url, "/v1/sessions/jwks/{project_id}"
        )

        res = self.sync_client.get(url, params=payload)
        return SessionsjwksResponse.from_json(res.response.status_code, res.json)

    async def SessionsJwks_async(
        self,
        project_id: str,
    ) -> SessionsjwksResponse:
        payload: Dict[str, Any] = {
            "project_id": project_id,
        }

        url = self.api_base.route_with_sub_url(
            self.sub_url, "/v1/sessions/jwks/{project_id}"
        )

        res = await self.async_client.get(url, params=payload)
        return SessionsjwksResponse.from_json(res.response.status, res.json)

    def MultiTenantSessionsJwks(
        self,
        project_id: str,
    ) -> MultitenantsessionsjwksResponse:
        payload: Dict[str, Any] = {
            "project_id": project_id,
        }

        url = self.api_base.route_with_sub_url(
            self.sub_url, "/v1/b2b/sessions/jwks/{project_id}"
        )

        res = self.sync_client.get(url, params=payload)
        return MultitenantsessionsjwksResponse.from_json(
            res.response.status_code, res.json
        )

    async def MultiTenantSessionsJwks_async(
        self,
        project_id: str,
    ) -> MultitenantsessionsjwksResponse:
        payload: Dict[str, Any] = {
            "project_id": project_id,
        }

        url = self.api_base.route_with_sub_url(
            self.sub_url, "/v1/b2b/sessions/jwks/{project_id}"
        )

        res = await self.async_client.get(url, params=payload)
        return MultitenantsessionsjwksResponse.from_json(res.response.status, res.json)

#!/usr/bin/env python3

from typing import Any, Dict, List, Optional

import requests
import aiohttp

from stytch.base import ApiBase
from stytch.http.client import AsyncClient, SyncClient
{% for sub_api in this.sub_apis %}
from stytch.{{ api_path_in_gen }}.{{ sub_api.filename_base }} import {{ sub_api.classname }}
{% endfor %}
{% for additional_import in this.additional_imports %}
{{ additional_import }}
{% endfor %}


class {{ this.classname }}:
    def __init__(self, api_base: ApiBase, sync_client: SyncClient, async_client: AsyncClient) -> None:
        self.api_base = api_base
        self.sync_client = sync_client
        self.async_client = async_client
        {% for sub_api in this.sub_apis %}
        self.{{ sub_api.filename }} = {{ sub_api.classname }}(sync_client, async_client)
        {% endfor %}

    @property
    def sub_url(self) -> str:
        return self.api_base.with_sub_url("{{ this.sub_url }}")

    {% for method in this.methods %}
    {{ method.generate()|indent(4) }}
    {% endfor %}
